name: Scrape FBref

on:
  workflow_dispatch:
    inputs:
      league:
        description: 'League code — leave blank for all (e.g. MEX-LMX, ENG-PL)'
        required: false
        default: ''
  schedule:
    - cron: '0 6 * * *'   # daily at 6:00 UTC

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python dependencies
        run: |
          pip install -r backend/requirements.txt

      - name: Run FBref scraper
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          SCRAPER_API_KEY: ${{ secrets.SCRAPER_API_KEY }}
        run: |
          cd backend
          if [ -n "${{ github.event.inputs.league }}" ]; then
            python -m scripts.scrape_fbref --league ${{ github.event.inputs.league }}
          else
            python -m scripts.scrape_fbref
          fi
